# Copy to .env.local and fill in values.
# Firebase (required for auth): get from Firebase Console > Project settings > General > Your apps
VITE_FIREBASE_API_KEY=
VITE_FIREBASE_PROJECT_ID=pornsite-49b56

# Optional: backend API base (defaults to http://localhost:5000 in dev)
# VITE_API_URL=http://localhost:5000
